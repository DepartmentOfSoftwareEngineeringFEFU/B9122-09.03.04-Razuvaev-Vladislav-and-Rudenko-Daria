spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sbom_analyzer
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  liquibase:
    change-log: classpath:/db.changelog/db.changelog-master.xml
    enabled: true

server:
  port: 8080
  tomcat:
    threads:
      max: 50